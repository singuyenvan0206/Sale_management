<Window x:Class="FashionStore.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Đăng Nhập - Hệ Thống POS" Height="500" Width="600"
        KeyDown="Window_KeyDown"
        Loaded="Window_Loaded">
    <Window.Resources>

        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="15"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="0"/>
        </Style>
        <!-- TextBox Style -->
        <Style TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="#4FC3F7"
                                BorderThickness="2"
                                CornerRadius="16">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
        <!-- PasswordBox Style -->
        <Style TargetType="PasswordBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="#4FC3F7"
                                BorderThickness="2"
                                CornerRadius="16">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="16"/>
        </Style>
    </Window.Resources>
    <Grid Background="#FAD2D2" Margin="0,0,0,-6">
        <!-- Login Form -->
        <Border x:Name="LoginPanel" CornerRadius="40"
                Padding="20" Height="NaN" Margin="75,44,75,23" Width="NaN">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#1CB5E0" Offset="0.0"/>
                    <GradientStop Color="#000046" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock Text="Đăng Nhập" FontSize="28" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,24"/>
                <TextBlock Text="Tên đăng nhập" Foreground="#CCFFFFFF" Margin="0,0,0,4"/>
                <TextBox x:Name="UsernameTextBox" Height="52" Margin="0,0,0,16"
                         Background="#F0F0F0" TabIndex="1"/>
                <TextBlock Text="Mật khẩu" Foreground="#CCFFFFFF" Margin="0,0,0,4"/>
                <PasswordBox x:Name="PasswordBox" Height="45" Margin="0,0,0,24"
                             Background="#F0F0F0" Width="293" TabIndex="2"/>
                <Button x:Name="LoginButton" Content="Đăng Nhập"
                        Width="297"
                        Height="45"
                        FontSize="18"
                        FontWeight="Bold"
                        Background="#4FC3F7"
                        Foreground="White"
                        BorderThickness="0"
                        Margin="0,0,0,12"
                        Click="LoginButton_Click"/>
                <Button x:Name="ChangePasswordButton" Content="Đổi Mật Khẩu"
                        Height="40"
                        FontSize="16"
                        Background="#FC5C7D"
                        Foreground="White"
                        BorderThickness="0"
                        Click="ChangePasswordButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>